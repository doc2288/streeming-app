## 2024-05-28 - ARIA Labels for Icon-Only Buttons
**Learning:** Found an accessibility issue pattern in the app's components where icon-only buttons (like those for deleting a stream or copying text) lacked ARIA labels.
**Action:** Applied translations to `aria-label` along with `title` attributes to improve screen reader accessibility.
