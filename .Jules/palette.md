## 2024-06-09 - Add aria-labels to icon-only buttons
**Learning:** The React UI had several icon-only buttons missing aria-labels (e.g. search clear, chat panel actions) which degrades screen reader accessibility.
**Action:** Add aria-label or title tags using existing translations, or create new translation strings where missing.
